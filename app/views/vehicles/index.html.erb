<div class="container">
  <div class="container-fluid ">
    <button class="navbar-toggler" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasDarkNavbar" aria-controls="offcanvasDarkNavbar">
      <span class="navbar-toggler-icon "></span>
    </button>
  </div>



  <% current_user.vehicles.each do |vehicle| %>
    <div class="card-vehicles">
      <div class= "row w-100 mt-2">
          <div class="col-4 text-center"><%= vehicle.brand %></div>
          <div class="col-4 text-center"><%= vehicle.model %></div>
          <div class="col-4 text-center"><%= vehicle.real_consumption %>€/L</div>
          <%= link_to "Edit Vehicle", edit_vehicle_path(vehicle) %>
      </div>
    </div>
  <% end %>
</div>
<%= link_to "Create Vehicle",  new_vehicle_path %>
<%= link_to "Back to Stations", stations_fuel_types_path(@fuel_type) %>
