<%= simple_form_for(@vehicle) do |f| %>
  <%= f.input :brand %>
  <%= f.input :model %>
  <%= f.input :real_consumption %>
  <%= f.submit %>
<% end %>


<%= link_to "Create Vehicle",  new_vehicle_path %>

<%= link_to "Delete",
  vehicles_path,
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
<%= link_to "Back to Stations", vehicles_path,  class: "btn btn-flat" %>
