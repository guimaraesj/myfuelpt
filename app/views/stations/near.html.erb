<%# faz sentido isto ser uma nova rota??
Não deveria ser no index das stations que seria logo feito o filtro? %>

<%# search within a radius of user location --> look for classes %>

  <div data-controller="map"
    id="map"
    data-map-api-key-value="<%= ENV["MAPBOX_API_KEY"]%>"
    data-map-marker-value="<%= @marker %>"
    style="width: 100%; height: 300px;">
  </div>

Station.near([user.lng, user.lat],)
<%# example from classes: %>
<%# Flat.near([40.71, 100.23], 20) # flats within 20 km of a point %>
